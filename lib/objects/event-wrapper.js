var config = require('../config');

// Libraries
var _ = require('lodash');

// Objects
var Event = require('./event');

/**
 * Holds and creates all events.
 */
class EventWrapper {
  constructor() {
  }
}