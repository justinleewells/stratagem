class StatusEffectInfo {
  constructor(params) {
    this.id = params.id
    this.name = params.name
    this.maxDuration = params.maxDuration
    this.maxStacks = params.maxStacks
  }
}
module.exports = StatusEffectInfo
